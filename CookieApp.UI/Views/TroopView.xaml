<UserControl x:Class="CookieApp.UI.Views.TroopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CookieApp.UI.Views"
             xmlns:viewModels="clr-namespace:CookieApp.UI.ViewModels"
             xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
             mc:Ignorable="d" 
             md:DialogServiceViews.IsRegistered="True"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Type={x:Type viewModels:TroopViewModel}}">
    <UserControl.Resources>
        <DataTemplate x:Key="GirlScoutListItemTemplate" DataType="{x:Type viewModels:GirlScoutViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding FullName}"/>
                <TextBlock Grid.Column="1" Text="{Binding ParentFullName}"/>
                <TextBlock Grid.Column="2" Text="{Binding PhoneNumber}"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Name}"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="Add Girl Scout" Command="{Binding AddGirlScoutCommand}"/>
            </StackPanel>
        </Grid>
        <ListBox ItemsSource="{Binding GirlScouts}"
                 ItemTemplate="{StaticResource GirlScoutListItemTemplate}"></ListBox>
    </StackPanel>
</UserControl>
